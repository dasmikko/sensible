export type GlobalArgsObject = {
    environment: string,
    verboseMode: boolean
    sensibleFolder: string
    sensibleFilePath: string
    localSensibleFolder: string,
    projectUser: string | null
}

export type rcObject = {
    projectUser: string
}